name: Lint 
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
   
    - name: Update dependencies
      run: |
        npm update
        npm install

    - name: Install ESLint
      run: npm install eslint
    
    - name: Run ESLint
      run: npx eslint